import { Placeholder } from '../components/Placeholder';

/** Generic stub for routes not yet built out past the foundation phase. */
export function RouteStub({ title }: { title: string }) {
  return <Placeholder title={title} />;
}
